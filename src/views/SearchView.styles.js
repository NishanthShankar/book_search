import {
  StyleSheet
} from 'react-native'

const styles = StyleSheet.create({
  searchBar: {
    flex: 1,
    height: 44,
    borderRadius: 4,
    paddingRight: 12,
    marginHorizontal: 12,
    flexDirection: 'row',
    backgroundColor: 'white',
    elevation: 3
  }
})

module.exports = styles
